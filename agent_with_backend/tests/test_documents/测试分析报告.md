# 测试分析报告

## 1 引言

### 1.1 编写目的

本报告记录 ROS2 智能药品拣选系统的自动化测试结果，评价 AI 医疗助手、认证授权、智能筛查、药品与库存、医生审批、订单流程及 ROS2 集成模块的当前质量，并为缺陷修复和后续 ROS2/Unity 联调提供依据。

预期读者为项目开发人员、测试人员、ROS2 集成人员及项目负责人。

### 1.2 被测对象

- **系统名称**：ROS2 智能药品拣选系统
- **被测主体**：`agent_with_backend`
- **测试范围**：`tests/` 下默认可发现的自动化测试，以及显式执行的 `test-full-integration.py`
- **未测试范围**：Unity、机器人硬件、安全渗透

### 1.3 测试环境

| 项目 | 实际环境 |
|---|---|
| **操作系统** | Ubuntu 24.04 |
| **Python** | 3.10.11 |
| **pytest** | 9.0.3 |
| **Flask** | 3.1.3 |
| **SQLite** | 3.40.1 |
| **ROS2** | 相关测试使用 Mock/替身 |

测试通过 Flask 测试客户端和临时 SQLite 数据库执行。

### 1.4 术语

| 术语 | 定义 |
|---|---|
| **API** | Application Programming Interface，应用程序接口 |
| **JWT** | JSON Web Token，接口认证令牌 |
| **ROS2** | Robot Operating System 2，机器人操作系统第二版 |
| **E2E** | End-to-End，端到端测试 |
| **Mock** | 用可控替身模拟外部依赖 |
| **xfail** | pytest 中预期失败且持续跟踪的已知缺陷 |

### 1.5 参考资料

- pytest、Flask、Python 官方文档
- `tests/test_documents/测试执行手册.md`
- `tests/test_documents/测试计划.md`

---

## 2 测试概要

### 2.1 测试文件

```bash
tests
├── test_documents
│   ├── 测试计划.md
│   ├── 测试分析报告.md
│   └── 测试执行手册.md
├── unit            # 单元测试
├── integration     # 集成测试
├── ros_integration # ros 相关集成测试
└── agent           # agent 相关测试
```

### 2.2 测试结果

默认执行 `pytest tests/` 共收集 380 项：

| 结果 | 数量 | 占比 |
|---|---:|---:|
| 通过 | 378 | 99.47% |
| 失败 | 0 | 0.00% |
| 预期失败（xfail） | 2 | 0.53% |
| 合计 | 380 | 100.00% |

不计 xfail 的实际执行通过率为 `378 / 378 = 100%`。最近一次全量覆盖率测试耗时 83.22 秒。

### 2.3 分组结果

| 测试集 | 结果 | 耗时 | 结论 |
|---|---|---:|---|
| `tests/unit` | 102 passed, 3 warnings | 13.47 秒 | 通过 |
| `tests/integration` | 14 passed, 2 xfailed, 30 warnings | 21.19 秒 | 无非预期失败，存在 2 个已知缺陷 |
| `tests/ros_integration` | 141 passed | 15.70 秒 | 通过 |
| `tests/agent` | 93 passed | 7.01 秒 | 通过 |
| 根目录 API 测试 | 28 passed, 40 warnings | 23.66 秒 | 通过 |

### 2.4 覆盖率

使用 `.coveragerc` 排除测试代码、CLI、初始化脚本等非统计范围后，生产代码共有 8746 条语句，未覆盖 2634 条，语句覆盖率为 **70%**。该结果较早期报告的 39% 提升 31 个百分点，但仍低于测试计划规定的系统整体 85% 目标。

覆盖较高的代表模块包括：

| 模块 | 覆盖率 |
|---|---:|
| `ros_integration/state_store.py` | 100% |
| `common/utils/symptom_db_service.py` | 100% |
| `api/upload_controller.py` | 100% |
| `database/connection.py` | 100% |
| `agent/session/manager.py` | 99% |
| `ros_integration/error_handler.py` | 95% |
| `ros_integration/message_adapter.py` | 95% |
| `common/utils/http_client.py` | 94% |
| `api/order_controller.py` | 85% |

覆盖不足的关键模块仍包括 `agent/engine/medical_agent.py`（21%）、`agent/subagents/form.py`（24%）、`auth/blueprint.py`（57%）、`ros_integration/bridge.py`（61%）和 `ros_integration/task_publisher.py`（61%）。

---

## 3 测试结果及发现

### 3.1 测试1（UT-001：药品 API 基础操作单元测试）

本项测试对药品控制器的创建、删除、库存调整、药品查询、低库存查询和临期药品查询进行验证。

| 比较项目 | 动态输出要求 | 实际动态输出 | 比较结果 |
|---|---|---|---|
| 用例执行 | 所有单元测试均成功执行 | 单元测试集 `102 passed, 3 warnings in 13.47s`，其中药品控制器 6 项基础用例全部通过 | 功能用例全部通过 |
| 创建药品 | 返回成功结果，并在数据库中生成药品数据 | 创建用例通过，临时 SQLite 数据库中生成对应药品记录 | 符合要求 |
| 查询药品 | 返回与输入条件匹配的药品数据 | 普通查询、低库存和临期查询用例全部通过 | 符合要求 |
| 库存调整 | 输出调整后的库存数据，数据库同步更新 | 库存调整用例通过 | 符合要求 |
| 执行时间 | 单元测试执行时间处于可接受范围 | 102 项单元测试耗时 13.47 秒 | 符合要求 |

**发现：**

1. 药品控制器的六项基础操作均得到预期动态输出，未发现功能失败。
2. 单元测试产生 3 条 SQLAlchemy 2.0 弃用警告，来自旧式 `declarative_base()` 导入方式；该问题未影响功能结果，但应在后续升级中修正。

### 3.2 测试2（IT-001～IT-005：API 集成测试）

本项测试覆盖认证生命周期、角色权限、智能筛查、筛查历史、药品与库存 API，以及不同模块之间的数据传递。

| 比较项目 | 动态输出要求 | 实际动态输出 | 比较结果 |
|---|---|---|---|
| 用例执行 | API 调用链路成功率不低于 95% | `14 passed, 2 xfailed, 30 warnings in 21.19s` | 无非预期失败，但有 2 项已知缺陷 |
| 认证生命周期 | 注册、登录、验证、刷新、注销输出正确，注销后刷新失败 | 对应请求依次返回成功；令牌注销后刷新请求返回 `401` | 符合要求 |
| 权限控制 | 患者、医生和管理员只能访问其权限范围内的接口 | 患者访问库存接口返回 `403`，患者审批操作返回 `403`，管理员可查询审计日志 | 符合要求 |
| 症状标准化 | “头疼”“发烧”转换为“头痛”“发热”，未知症状单独返回 | 输出包含标准症状 `头痛`、`发热`，并将“未知症状”列入未匹配项 | 符合要求 |
| 智能筛查 | 返回不超过指定数量的候选药品，并按置信度降序输出 | 返回 1～3 条结果，首项置信度不低于末项，历史记录成功保存 | 符合要求 |
| 审计完整性 | 认证和业务操作生成可查询的审计数据 | 登录操作出现在审计日志中；审批自动下单缺少库存流水 | 部分符合 |

**发现：**

1. 认证、权限、筛查和历史记录之间的数据传递正确，主要 API 集成链路可用。
2. `test_approval_dispatch_writes_inventory_transaction` 为严格 xfail。实际输出表明审批自动下单会扣减库存，但没有生成 `inventory_transactions` 记录，库存审计链不完整，记为 DEF-INT-001。
3. `test_recent_ros_state_reports_connected` 为严格 xfail。生产代码仍使用 Python 3.10 无法直接解析的尾缀 `Z` 时间戳，最新 ROS 状态经 HTTP 查询时可能输出 `connected=false`。

### 3.3 测试3（E2E-001：完整用药流程测试）

本项测试验证患者提交审批、医生处理、库存扣减、订单生成、任务发布调用和患者确认完成之间的完整业务流程。

| 比较项目 | 动态输出要求 | 实际动态输出 | 比较结果 |
|---|---|---|---|
| 患者提交审批 | 返回审批编号并保存待审批数据 | 请求返回 `201`，生成 `approval_id` | 符合要求 |
| 医生待办查询 | 待审批列表中包含新生成的审批编号 | 查询返回 `200`，列表包含对应 `approval_id` | 符合要求 |
| 医生批准 | 审批状态更新并创建订单任务 | 请求返回 `200`，`order_created=true`，生成 `task_id` | 符合要求 |
| 库存变化 | 批准数量为 2 时，库存由 10 减少至 8 | 数据库实际库存为 8 | 符合要求 |
| 任务发布 | 发布函数接收正确的任务、药品和数量 | Mock 捕获输出为 `(task_id, drug_id, 2)` | 符合要求 |
| 流程完成 | 患者确认后审批状态变为 `completed` | 完成请求返回 `200`，最终状态为 `completed` | 符合要求 |
| 事务原子性 | 批量订单任一项目无效时全部回滚 | 库存分别保持 10 和 1，订单、库存流水及发布调用数量均为 0 | 符合要求 |
| 直接订单审计 | 扣库、订单和库存流水数据一致 | 库存由 6 变为 4；订单数量为 2；流水变化量为 `-2` | 符合要求 |

**发现：**

1. 患者、医生、库存、订单和任务发布调用之间的主业务链路能够完成，内部生成的审批编号和任务编号可在后续步骤继续使用。
2. 批量订单在库存不足时能够整体回滚，没有产生部分扣库或无效任务。
3. 直接订单能够生成库存流水，但审批自动下单未生成同类流水，因此两条下单路径的动态输出不一致。
4. 显式执行旧文件 `test-full-integration.py` 时得到 `2 passed`，但两个测试函数通过返回布尔值报告结果，pytest 同时输出 `PytestReturnNotNoneWarning`。该文件存在流程失败后仍被判为通过的风险，不能单独作为正式 E2E 验收依据。

### 3.4 测试4（E2E-004：ROS2 集成测试）

本项测试使用 Mock/替身验证 ROS 配置、节点管理、消息转换、状态存储、健康监控、错误处理、任务发布和返回队列命令。

| 比较项目 | 动态输出要求 | 实际动态输出 | 比较结果 |
|---|---|---|---|
| 独立测试集执行 | ROS2 集成用例成功率不低于 95% | `141 passed in 15.70s`，成功率 100% | 符合要求 |
| 消息格式转换 | 药品、货架、任务和小车状态正确转换 | 消息适配相关测试全部通过 | 符合要求 |
| 错误与降级处理 | 发布异常产生重试或降级结果，系统不崩溃 | 错误处理和健康监控测试通过 | 符合 Mock 测试要求 |

**发现：**

1. ROS2 Mock 测试已补齐 QoS、消息类型、发布器、订阅器和单例重置逻辑，原有非预期失败及顺序依赖未再出现。
2. 状态订阅回调、数据库状态更新、车辆状态和药柜状态均已增加测试；`state_subscriber.py` 覆盖率由 17% 提升至 63%。
3. `updated_at` 尾缀 `Z` 的生产代码兼容问题仍由严格 xfail 跟踪，记为 DEF-ROS-001。
4. 本项仅验证 Mock/替身环境，尚未取得真实 ROS2 话题、ROS-TCP-Endpoint、Unity 或机器人产生的动态输出。

### 3.5 测试5（UT-002～UT-005：Agent 及辅助模块测试）

本项测试覆盖 LLM 消息结构、MedicalAgent 基础行为、消息记忆、待办管理、工具注册执行和工作流状态管理。

| 比较项目 | 动态输出要求 | 实际动态输出 | 比较结果 |
|---|---|---|---|
| 用例执行 | Agent 基础模块测试全部成功 | `93 passed in 7.01s` | 符合要求 |
| 消息与工具数据 | 序列化、反序列化和工具调用字段保持一致 | LLM schema 和工具测试全部通过 | 符合要求 |
| 记忆管理 | 消息添加、重置和压缩后保留规定内容 | 记忆管理测试全部通过 | 符合要求 |
| 待办与工作流 | 状态变化、进度、统计和重置输出正确 | 待办及工作流测试全部通过 | 符合要求 |
| 异步调用 | 异步 Mock 应被正确等待 | 本次分组执行未产生协程未等待警告 | 符合要求 |

**发现：**

1. Agent 基础数据结构和内部状态输出与测试要求一致，未出现功能失败。
2. LLM Client、OpenAI/Claude Provider、流式输出及网络异常分支已补充测试；`agent/llm/client.py` 覆盖率达到 98%。
3. 本项主要验证 Mock 环境中的基础行为，未验证真实 LLM 的医学准确率、响应时间和长对话稳定性。

### 3.6 测试6（SYS-001：全量回归与覆盖率测试）

本项测试比较整个自动化测试集的执行结果和生产代码覆盖率是否达到测试计划规定的系统准入要求。

| 比较项目 | 动态输出要求 | 实际动态输出 | 比较结果 |
|---|---|---|---|
| 全量回归 | 所有非预期测试成功执行 | `378 passed, 2 xfailed, 73 warnings in 83.22s` | 符合要求 |
| 非 xfail 通过率 | 不低于 95% | `378 / 378 = 100%` | 达到比例要求 |
| 生产代码覆盖率 | 不低于 85% | 70% | 不符合要求 |
| HTML 报告 | 生成可查看的覆盖率 HTML 报告 | 生成 `coverage_report/index.html` | 符合要求 |

**发现：**

1. 全量测试没有非预期失败，两个严格 xfail 均对应已登记缺陷。
2. 生产代码覆盖率为 70%，与 85% 目标相差 15 个百分点。下一阶段应优先补测 `medical_agent.py`、`subagents/form.py`、认证蓝图和 ROS 发布流程。
3. HTTP Client、订单控制器、配置服务、上传接口、数据库连接和症状数据库服务已达到 85%～100% 覆盖率。
4. 覆盖率 HTML 报告已生成，可按文件和缺失行继续定位测试缺口。

---

## 4 对软件功能的结论

### 4.1 认证与权限

注册、登录、刷新、注销、JWT 校验和角色权限测试通过，核心权限边界可用。但测试密钥长度不足，尚未执行令牌过期等待、密钥轮换、并发会话和渗透测试。

### 4.2 智能筛查与 Agent

症状标准化、同义词扩展、药品筛选、批量筛查、配置管理、历史记录，以及 Agent 的消息、记忆、工具和工作流基础能力通过测试。筛查配置服务覆盖率达到 96%，筛查路由覆盖率达到 79%。

### 4.3 药品、库存、审批与订单

药品 CRUD、查询、分类、库存调整、文件上传和主要审批下单流程可执行，批量订单的事务原子性得到验证。订单控制器覆盖率达到 85%，但审批自动下单仍未生成库存流水。

### 4.4 ROS 状态与任务接口

配置、消息转换、状态订阅、任务发布、错误处理和健康监控的大部分逻辑通过 Mock 测试，ROS2 独立测试集 141 项全部通过。本次未验证真实 Unity 通信和硬件故障恢复。

---

## 5 分析摘要

### 5.1 能力

经测试，本软件具有如下能力：
1. Agent 的消息模型、记忆压缩、待办管理、工具注册和工作流状态管理等功能完善；
2. 用户注册、登录、令牌刷新与注销、角色权限、筛查历史和关键 API 权限边界等功能完善；
3. 药品 CRUD、搜索、分页、批量导入、导出、分类和库存调整等功能完善。
4. 患者提交审批、医生审批、扣减库存、创建任务和患者确认完成的主要业务等功能完善。
5. ROS 配置、错误处理、健康监控、消息适配、节点管理等功能完善。
6. HTTP 客户端、订单边界、上传接口、筛查配置和症状数据库服务已覆盖正常、异常和降级路径。

### 5.2 缺陷与限制汇总

| 编号 | 等级 | 缺陷或限制 | 建议 |
|---|---|---|---|
| DEF-ROS-001 | P2 | `Z` 时间戳解析失败，在线状态误判 | 解析前将 `Z` 转为 `+00:00`，并补充时区测试 |
| DEF-INT-001 | P2 | 审批自动下单未写库存流水 | 复用统一订单事务逻辑，保证库存、订单、流水原子提交 |
| DEF-TEST-001 | P2 | 生产代码覆盖率为 70%，未达到 85% 目标 | 优先补齐 MedicalAgent、表单状态机、认证蓝图和 ROS 发布流程 |
| DEF-TEST-002 | P3 | E2E 文件未被默认发现且返回布尔值 | 重命名为 `test_full_integration.py` 并改用断言 |
| DEF-SEC-001 | P2 | JWT 测试密钥长度不足 | 使用至少 32 字节的测试与部署密钥 |
| DEF-DEP-001 | P3 | Screening ORM 使用 SQLAlchemy 2.0 已弃用的 `declarative_base()` 导入 | 改用 `sqlalchemy.orm.declarative_base` 或统一 DeclarativeBase |
| DEF-ENV-001 | P3 | 尚未执行真实 ROS2、Unity 和机器人端到端测试 | 在目标部署环境补充真实话题与故障恢复验证 |

### 5.3 建议

系统的 Agent 基础能力、认证授权、药品库存 API、订单、筛查配置和主要审批流程已具备继续开发与联调的基础。当前自动化测试已无非预期失败，但仍存在审批库存流水缺失、ROS 时间戳兼容和关键大模块覆盖不足等问题。

修复后应在相同 Python 3.10 环境重跑全部 380 项默认测试及显式 E2E 测试，并在 Ubuntu ROS2 Humble、ROS-TCP-Endpoint 和 Unity 环境补做真实消息端到端验证。

### 5.4 评价

| 准入项 | 目标 | 实际 | 结论 |
|---|---:|---:|---|
| 全量测试 | 95% 成功 | 378 passed，0 failed，2 xfailed | 通过 |
| 集成测试通过率 | ≥95% | 14 passed，2 个已知 xfail | 通过 |
| ROS2 集成成功率 | ≥85% | 独立 Mock 测试 141/141，100% | 通过 |
| 生产代码覆盖率 | ≥85% | 70% | 不通过 |
| P1 缺陷 | 0 | 0 | 通过 |
| P2 缺陷 | ≤5 | 4 项 | 通过 |
| 审计日志完整 | 完整 | 审批自动下单缺少库存流水 | 部分通过 |

该软件已达到自动化回归稳定、主要业务链路可联调的阶段，但覆盖率和审计完整性尚未达到全部准入目标，建议完成剩余 P2 问题并进行真实 ROS2/Unity 验证后再判定正式交付。


---

## 6 测试资源消耗

本次数据来自当前工作区自动化测试执行结果。机时消耗如下：
| 项目 | 实际消耗 |
|---|---|
| 默认全量测试 | 380 项，其中 378 passed、2 xfailed |
| 覆盖率测试 | 380 项，83.22 秒 |
| 分组测试 | unit、integration、ros_integration、agent、根目录 API/E2E |
| 数据资源 | 测试夹具创建临时 SQLite 数据库 |
| 报告产物 | 本 Markdown 报告；`coverage_report/index.html`；`report.html` |
